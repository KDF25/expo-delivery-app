export enum ENUM_SECURE_STORE {
	ACCESS_TOKEN = "accessToken",
	REFRESH_TOKEN = "refreshToken"
}

export enum ENUM_ASYNC_STORAGE {
	USER = "user"
}

export enum ENUM_AUTH_TYPE {
	LOGIN = "login",
	REGISTRATION = "registration"
}
